apiVersion: v1
kind: Service
metadata:
  name: robot-control-service
  namespace: default  # Add this line
spec:
  type: NodePort
  selector:
    app: robot-control
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005
      nodePort: 30005
